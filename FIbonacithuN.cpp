#include <iostream>
#include <string>
#include <vector>
int main()

{
	
	vector <string> s;
	
	for(auto n:s)
	{
		if(n != ' ') buff+=n; else
//		if(n == c && buff != "") 
	{ v.push_back(buff); buff = ""; }
	}
	if(buff != "") v.push_back(buff);
	
	for (auto n:v )
	cout<<v;
}
